// Generated by CoffeeScript 1.4.0
(function() {

  define(['jqueryUI', 'underscore', 'EventEmitter'], function($, _, event_emitter) {
    var UserPhotos;
    return UserPhotos = (function() {

      function UserPhotos() {
        this.fb_init();
      }

      UserPhotos.prototype.fbLogin = function() {
        return FB.login(function(response) {
          if (response.authResponse) {

          }
        });
      };

      UserPhotos.prototype.loadPhotoCollection = function(content, source, cb) {
        console.log("load collection: " + content + ", " + source);
        if (content === 'facebook') {
          this.load_fb_photo_collection(source, cb);
        }
      };

      UserPhotos.prototype.getCollectionPhotos = function(content, source, id, cb) {
        if (content === 'facebook') {
          return this.get_facebook_collection_photos(source, id, cb);
        }
      };

      UserPhotos.prototype.get_facebook_collection_photos = function(source, id, cb) {
        return FB.api("/" + id + "/photos", cb);
      };

      UserPhotos.prototype.load_fb_photo_collection = function(source, cb) {
        if (source === 'me') {
          this.load_fb_user_photos(cb);
        }
        if (source === 'albums') {
          return this.load_fb_albums(cb);
        }
      };

      UserPhotos.prototype.load_fb_user_photos = function(cb) {
        console.log("load fb_user");
        return FB.api('me/photos', function(response) {
          var user_images;
          user_images = _.map(response.data, function(img) {
            return {
              id: img.id,
              photo_url: img.picture,
              images: img.images
            };
          });
          return cb({
            images: user_images
          });
        });
      };

      UserPhotos.prototype.load_fb_albums = function(cb) {
        var _this = this;
        FB.api('/me/albums?fields=id,name,cover_photo', function(albums) {
          var album_data, i, k, _i, _ref;
          album_data = _.map(albums.data, function(alb) {
            return {
              id: alb.id,
              name: alb.name
            };
          });
          for (i = _i = 0, _ref = album_data.length - 1; 0 <= _ref ? _i <= _ref : _i >= _ref; i = 0 <= _ref ? ++_i : --_i) {
            k = 0;
            FB.api("/" + albums.data[i].cover_photo, function(cover_response) {
              album_data[k].cover_url = cover_response.picture;
              if (k === album_data.length - 1) {
                cb({
                  "collection": album_data
                });
                return;
              }
              k++;
            });
          }
        });
      };

      UserPhotos.prototype.fb_init = function() {
        var fbLogin;
        fbLogin = this.fbLogin;
        window.fbAsyncInit = function() {
          FB.init({
            appId: '236634053108854',
            channelUrl: '//localhost/channel.html',
            status: true,
            cookie: true,
            xfbml: true
          });
          return FB.getLoginStatus(function(response) {
            if (response.status === 'connected') {
              console.log("facebook connected");
              return event_emitter.emit('facebook:connected');
            } else {
              return fbLogin();
            }
          });
        };
        return (function(d) {
          var id, js, ref;
          id = 'facebook-jssdk';
          ref = d.getElementsByTagName('script')[0];
          if (d.getElementById(id)) {
            return;
          }
          js = d.createElement('script');
          js.id = id;
          js.async = true;
          js.src = '//connect.facebook.net/en_US/all.js';
          return ref.parentNode.insertBefore(js, ref);
        })(document);
      };

      return UserPhotos;

    })();
  });

}).call(this);
